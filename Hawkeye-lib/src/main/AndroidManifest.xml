<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dianping.vi_lib">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application>
        <provider
            android:name=".HawkeyeInitProvider"
            android:authorities="${applicationId}.ViewInspectorInitProvider"
            android:exported="false" />
        <service android:name=".suspend.DebugSuspendViewService" />

    </application>
</manifest>